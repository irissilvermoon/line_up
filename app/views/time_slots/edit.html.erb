<h1> Editing Time Slot </h1>

<%= render 'form' %>

<%= link_to "Update Time Slot", club_night_event_time_slot_path(@club_night, @event, @time_slot) %> |
<%= link_to "Back to #{@event.name}", club_night_event_path(@club_night, @event) %>