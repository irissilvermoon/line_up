<%= simple_form_for([@time_slot, @booking], html: {:class => 'large-4'}) do |f| %>
  <%= f.input :dj_id, label: "DJ", collection: @djs, label_method: :dj_name,
              value_method: :id, prompt: "Select a DJ",  input_html: { multiple: true } %>
  <%= f.button :submit %>
<% end %>


