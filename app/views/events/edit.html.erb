<h1>
  <%= "Editing #{@event.name}" %>
</h1>

<%= render 'form' %>

<%= link_to "Update Event", club_night_event_path(@club_night, @event) %>
<%= link_to "Back to #{@club_night.name}", club_night_path(@club_night) %>